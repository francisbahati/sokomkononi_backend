from django.apps import AppConfig


class SavedConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.saved"
    label = "saved"
    verbose_name = "Zilizohifadhiwa"