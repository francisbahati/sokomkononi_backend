from django.apps import AppConfig


class ReservationRatesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.reservation_rates"
    label = "reservation_rates"
    verbose_name = "Viwango vya Reservation"
